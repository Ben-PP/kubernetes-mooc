# Todo app
Small todo backend that saves todos and responds them.

## Environment
| Name | Example | Description |
| :--: | :--: | :--- |
| `POSTGRES_USER` | `postgres` | Username for the database. |
| `POSTGRES_HOST` | `example.com` | Hostname or ip address of the postgres db. |
| `POSTGRES_PORT` | `5432` | Port where the db is running. Defaults to `5432`. |
| `POSTGRES_PASSWORD` | `supersecretpassword` | Password of the postgres user. |
| `POSTGRES_DATABASE` | `postgres` | Database to connect to. |

## Deployment
To deploy, checkout the [manifests](https://github.com/Ben-PP/kubernetes-mooc/tree/2.8/manifests/the-project).
